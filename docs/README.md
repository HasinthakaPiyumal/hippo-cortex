# `docs/`

Design docs, protocols, hardware notes — things that aren't code and aren't literature review.

| File                    | What it is                                                                            |
| ----------------------- | ------------------------------------------------------------------------------------- |
| `architecture.md`       | HippoCortex architecture spec. Ground truth for what each module does.                |
| `hardware-go2edu.md`    | Compute / sensor budget for the Unitree Go2 Edu; SDK + ROS2 notes.                    |
| `timeline.md`           | Month-by-month plan copied from the proposal; updated as reality shifts.              |

Add more docs as they become load-bearing (e.g. `eval-protocol.md`, `robot-safety.md`, `data-collection.md`). Keep them short — if a doc would be 50+ pages, split it.
