# HippoCortex — pip requirements
#
# DO NOT run this file directly with pip install -r requirements.txt first.
# mamba-ssm and causal-conv1d import torch at build time, so PyTorch must
# already be installed before this file is used.
#
# Correct install order:
#   1. pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 \
#          --index-url https://download.pytorch.org/whl/cu121
#   2. pip install causal-conv1d==1.4.0
#   3. pip install mamba-ssm==1.2.2
#   4. pip install -r requirements.txt
#   5. pip install -e .
#
# mamba-ssm and causal-conv1d are intentionally NOT listed here.
# They must be installed in steps 2-3 above while torch is already present.

# ── Core numerical stack ──────────────────────────────────────────────
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0

# ── Config & experiment tracking ──────────────────────────────────────
omegaconf==2.3.0
hydra-core==1.3.2
wandb==0.17.3

# ── Deep learning utilities ───────────────────────────────────────────
einops==0.8.0
torchmetrics==1.4.0
timm==1.0.9
transformers>=4.40.0

# ── Data ─────────────────────────────────────────────────────────────
continuum==1.2.7

# ── Visualisation ────────────────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2

# ── Utilities ────────────────────────────────────────────────────────
tqdm==4.66.4
pyyaml==6.0.1
opencv-python==4.10.0.84

# ── Testing ──────────────────────────────────────────────────────────
pytest==8.2.2
